/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['res.cloudinary.com'], // Add the domain of your image source here
      },
};

export default nextConfig;
